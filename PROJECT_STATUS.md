# حالة المشروع — يسر (sakan-360)

آخر تحديث: 2026-09-05 · للرجوع السريع في أي جلسة قادمة، بشرية كانت أو مع Claude.

الموقع الحيّ: **https://sakan-360.vercel.app**
المستودع: **https://github.com/hussain-alayfei/yoser** (فرع `master` وحيد، النشر تلقائي عند push)

---

## 1. ما هو يسر

منصة رحلة سكنية للمستفيد السعودي: من تعبئة البيانات، لمعرفة البرامج المرشّحة، لتقديم الطلب ومتابعته، لمتابعة بناء المسكن بتوأم رقمي، لمرحلة الصيانة بعد الاستلام. ولها مساحة موازية للجمعية الشريكة تراقب الحالات وتتدخّل قبل أن يشتكي المستفيد.

**الهدف الأساسي لهذه الجولة:** إضافة ذكاء اصطناعي حقيقي (لا واجهات ثابتة) لثلاث مشاكل شغل حقيقية، مع ضمانة صارمة: **الذكاء الاصطناعي لا يقرر الأهلية أبدًا** — يعطي ترشيحًا وتفسيرًا فقط، والقرار النهائي للجهة المختصة.

**الستاك:** React 19 + Vite + TypeScript + Tailwind 4، Express + tRPC + Drizzle (خادم كامل غير منشور حاليًا)، ونشر Vercel كموقع ثابت + دوال serverless في `api/`.

---

## 2. الميزات الثلاث — منفَّذة ومُتحقَّق منها حيّة

كل ميزة: قاعدة محلية (rules) تُعرض فورًا، ثم تُستبدل بردّ `gpt-5-mini` إن نجح الاتصال. أي فشل — لا مفتاح، انقطاع، مهلة — يُبقي القاعدة ظاهرة بهدوء دون كسر الواجهة. الشارة تُظهر المصدر: **"تحليل ذكي"** أو **"قواعد أساسية"**.

### الميزة 1 — اقتراح البرامج المناسبة (`/programs`)
- الملفات: [api/ai/recommend-programs.ts](api/ai/recommend-programs.ts) · [BeneficiaryPages.tsx `ProgramsPage`](client/src/pages/BeneficiaryPages.tsx)
- المخطط له 4 قيم ملاءمة فقط (مرتفعة/متوسطة/تحتاج تحقق/غير مناسب) — **لا يوجد حقل "مؤهل"**. مضمون في 3 طبقات: صياغة الموجّه، بنية JSON Schema (`strict:true`)، وحارس نصّي (regex) يستبدل أي كلمة قرار تتسرّب.
- كانت ترسل بيانات الدخل/الدعم/المصروفات من سجل تجريبي وليس من نموذج `/start` الفعلي — **تم إصلاحه**: تُرسَل الآن كـ"غير متوفر" حين لا تكون محصّلة فعلًا، والنموذج يذكر ذلك صراحة كسبب لـ"يحتاج تحقق إضافي".

### الميزة 2 — اكتشاف الطلبات المتوقفة (`/association`)
- الملفات: [api/ai/stalled-cases.ts](api/ai/stalled-cases.ts) · [Association.tsx `StalledCasesPanel`](client/src/pages/Association.tsx)
- خمس إشارات مطلوبة، كلها تعمل الآن وتم التحقق منها حيّة:
  1. بقاء أطول من المعتاد في مرحلة
  2. لا تحديث منذ مدة
  3. مستند مكتمل لكن الطلب لم يتحرك
  4. تحويل الحالة أكثر من مرة
  5. تواصل المستفيد أكثر من مرة بلا تقدّم
- **باگ حقيقي كان موجودًا ووُجد بالتدقيق**: الإشارتان 4 و5 كانتا تُشتقّان بمطابقة نصية على عناوين السجل (`تحويل`/`تواصل`)، ولم يكن أي عنوان في البيانات يحتوي هاتين الكلمتين، فكانتا صفرًا دائمًا — أي شيفرة ميتة. أُصلح بجعلهما حقلين صريحين على `AssociationCase` في [data.ts](client/src/data.ts)، مع اختبار انحدار في [stalled-signals.test.ts](server/stalled-signals.test.ts) يضمن أن كل إشارة تُطلَق فعلًا.
- الأرقام تُحسَب في الخادم قبل النموذج (`detectSignals`)؛ النموذج يرتّب الأولوية ويكتب السبب والإجراء فقط، فلا يخترع أرقامًا.
- كانت "قيد المراجعة" تُحسب كمتطلب معطِّل رغم أنها الحالة الطبيعية بعد رفع مستند — **أُصلح**، فقط "ناقص" يُعتبر تعطيلًا.

### الميزة 3 — تحديد الخطوة التالية
- الملفات: [api/ai/next-step.ts](api/ai/next-step.ts) · مكوّن `AiNextAction` داخل [BeneficiaryPages.tsx](client/src/pages/BeneficiaryPages.tsx)
- يقرأ المرحلة + المستندات + آخر إجراء + حالة الملف، ويعيد إمّا إجراءً واحدًا أو "لا يوجد إجراء مطلوب منك حاليًا — الطلب لدى [الجهة]" صراحة.
- **كانت المشكلة الأهم**: المكوّن كان يظهر فقط بعد التخصيص (`/unit`, `/unit/maintenance`) وليس في `/home` حيث يُطرح سؤال "وش أسوي الحين؟" فعليًا — وكان `/home` يعرض بطاقة ثابتة hardcoded بدل الذكاء الاصطناعي. **أُصلح**: يظهر الآن في `/home` قبل وبعد إنشاء الطلب، ويقرأ موضع المستخدم الحقيقي من `sessionStorage` بدل قيم ثابتة وقت الترجمة (كانت المرحلة "التخصيص" ورقمها 5 دائمًا بغضّ النظر عن التقدّم الفعلي).
- حين لا يُطلب إجراء، الزر يتحول تلقائيًا لرابط ثانوي بدل زر أساسي بارز يناقض النص.

---

## 3. الأمان والمعمارية

- **المفتاح لا يصل للمتصفح إطلاقًا.** `OPENAI_API_KEY` يعيش فقط في متغيرات بيئة Vercel، ويُقرأ داخل دوال `api/ai/*.ts` (Vercel Serverless Functions، ليست خادم Express — دُوال منفصلة تُترجم وتُشغَّل كـ ESM أصلية، لذلك كل استيراد داخل `api/` يحتاج امتداد `.js` صريح).
- بدون المفتاح: كل نداء يعيد `503 {notConfigured:true}` بهدوء والواجهة تستمر بالقواعد.
- التخزين المؤقت: نتائج الذكاء الاصطناعي تُخزَّن في الجلسة بمفتاح = المسار + بصمة المدخلات، فالتنقّل جيئة وذهابًا لا يُعيد استدعاء النموذج بلا داعٍ (موثّق تجريبيًا: 5 تنقّلات → نداءان فقط).
- **الموديل `gpt-5-mini` لم يُستدعَ فعليًا حتى الآن** — كل التحقق تم حتى فحص المفتاح فقط (503 صحيح). أول استدعاء حقيقي سيحدث بعد إضافة المفتاح، ويستحق مراقبة.

---

## 4. حالة النشر — ما تحتاج فعله أنت

في Vercel → مشروع sakan-360 → Settings → Environment Variables، أضف:
```
OPENAI_API_KEY = <مفتاحك>
```
على Production + Preview، ثم أعد النشر (`vercel deploy --prod` أو أي push جديد). الشارات ستتحول من "قواعد أساسية" إلى "تحليل ذكي" فور نجاح أول نداء.

---

## 5. التصميم — ما تغيّر ولماذا

طُلب تجنّب "الشكل المولَّد بالذكاء الاصطناعي" (AI slop): خطوط Inter افتراضية، تدرّجات بنفسجية، بطاقات ظليّة موحّدة، زوايا كبيرة عشوائية.

- **الخط**: تحوّل من Tajawal/IBM Plex Sans Arabic إلى **Almarai** بكل أوزانه (300/400/700/800) في كل الملفات.
- **الأسطح**: أُزيلت كل الظلال العائمة والتدرّجات الداخلية للبطاقات — الآن حدّ 1px بدل box-shadow (0 عنصر مظلَّل متبقٍّ، تم التحقق برمجيًا حيّة).
- **الزوايا**: سلّم ضاق من 26 قيمة عشوائية (5–26px) إلى 5 درجات مقصودة (3/5/8/10/12px)، معرّف في [design.css](client/src/design.css).
- **الحركة**: مدد متفرّقة (.14/.16/.17/.18/.2s) وُحّدت لسلّم واحد (`--dur-1/2/3` في [index.css](client/src/index.css))، وأُضيف `--ease-out` الذي كان **مُستخدَمًا في كل مكان ولم يكن مُعرَّفًا أصلًا** (باگ حقيقي، fallback صامت لسنوات محتملة).
- **باگ التراكب الذي أبلغ عنه المستخدم بصورة**: صفّ البطاقات الثلاث في `/home` («الخطوة الأولى: بياناتك» إلخ) كان كله معلَّقًا على فئة CSS اسمها `.metric-grid` — وهذه الفئة **لا تظهر في أي ملف tsx إطلاقًا**. فكانت البطاقات تتدفّق كأزرار `inline-block` عادية وتتراكم فوق بعضها. أُصلح ببناء الشبكة على الحاوية الفعلية (`.metrics-section`) مباشرة، ونفس الباگ كان في لوحة `/staff`.
- **ترتيب استيراد حرِج**: `design.css` يُستورَد من `main.tsx` بعد `index.css` — وليس من `App.tsx` — لأن وحدات ES تُقيَّم قبل استيراداتها، فلو بقي في `App.tsx` لسبقه `index.css` في الحزمة وكسب `:root` القديم.
- تحقُّق مباشر: 10 صفحات، 390px و1265px، صفر تراكب، صفر تمرير جانبي، صفر ظلال متبقية.

**اتجاه بصري**: منصّة حكومية تحريرية، لا لوحة SaaS استهلاكية. حدّ بدل ظل، تسلسل بالوزن الطباعي بدل التغليف بصناديق.

---

## 6. باگات أخرى أُصلحت هذه الجولة

- Favicon كان يشير لصورة محذوفة من `/manus-storage/` (404 في كل تحميل).
- `viewport` كان يمنع pinch-zoom (`maximum-scale=1`) — مخالفة وصول WCAG 1.4.4.
- القائمة الجانبية تبقى `inert` (غير قابلة للوصول بلوحة المفاتيح) بعد تجاوز نقطة التوقف 760px دون إعادة تحميل — رُصد حيًّا وأُصلح بمستمع `resize` إضافي.
- لا وجود لعلامة يسر على الجوال إطلاقًا (تعيش في القائمة الجانبية المخفية) — أُضيفت نسخة مصغّرة في الشريط العلوي للجوال فقط.
- أربع صور مفقودة (`/manus-storage/*`) استُبدلت بـ SVG وتدرّجات CSS مُستضافة ذاتيًا.
- وسم تحليلات كان يطلب مسارًا حرفيًا `%VITE_ANALYTICS_ENDPOINT%/umami` غير موجود (404 في كل تحميل) — عُطِّل بتعليق موثَّق.

---

## 7. تدقيق مستقل — 30 وكيل، ماذا وجد

بعد التنفيذ الأول، شغّلت تدقيقًا عدائيًا (3 مدقّقين متوازيين + تحقّق مستقل لكل فجوة مزعومة). **20 فجوة مؤكدة، 7 ادعاءات مرفوضة**. أهمها أُصلح (مذكور أعلاه في الأقسام 2–3). المتبقي أقل خطورة:

- الإشارة (ج) في الميزة 2 هي مجموعة جزئية صارمة من (ب) — تفصيل منطقي بلا تأثير عملي حاليًا.
- حقل `match` في بيانات البرامج ميت (يُكتب ولا يُقرأ) — تنظيف مؤجَّل.
- لا اختبار انحدار مباشر لضمانة "لا قرار أهلية" في الميزة 1 (الضمانة موجودة، لكن غير محروسة باختبار).
- `pnpm dev` المحلي لا يخدم `/api/*` — لا يوجد proxy في `vite.config.ts` ولا mount في خادم Express؛ الاختبار المحلي للذكاء الاصطناعي غير ممكن حاليًا، فقط على Vercel.

---

## 8. التوأم الرقمي الحالي — كيف صُنع (CSS فقط، بلا مكتبات)

يعيش في [UnitTwin.tsx](client/src/components/UnitTwin.tsx) و[upgrade.css](client/src/upgrade.css)، ظاهر في `/unit`. **ليس Three.js ولا WebGL** — تقنية CSS 3D بحتة:

| العنصر | الطريقة |
| --- | --- |
| الكاميرا | `perspective: 1000px` على الحاوية الأب |
| السماح بالعمق | `transform-style: preserve-3d` على كل عنصر في السلسلة |
| الصندوق (دور) | 6 عناصر `<div>` بأحجام مشتقّة من نصف العمق، مُدارة بـ `translateZ`/`rotateY`/`rotateX` |
| الإضاءة | مزيّفة بمتغيرات CSS لكل وجه (`--front`/`--side`/`--top`) — لا إضاءة حقيقية |
| السحب للتدوير | `pointerdown/move/up` + `setPointerCapture`، تحويل الإزاحة الأفقية لزاوية `rotateY` |
| النقر على عنصر | حدث onClick عادي على كل `<button>` دور |

**الحد الأقصى لهذا الأسلوب**: لا إضاءة حقيقية، لا ظلال محسوبة، لا كاميرا متحركة (lerp)، يتعطّل بصريًا بعد ~30–50 وجه أو مع أشكال متقاطعة معقّدة. جيد لمخطط مكدَّس بسيط، لا لمشهد ثلاثي أبعاد حقيقي.

---

## 9. المشروع السابق (jawaz-al-imtithal) — كيف صُنع تويست ثلاثي أبعاد احترافي هناك

هذا **ليس موجودًا في هذا المستودع** — معرفة منقولة من ملاحظة المستخدم فقط، لا كود يمكن نسخه هنا.

**الستاك**: React 19 + TypeScript + Vite + **Three.js** + **@react-three/fiber** + **@react-three/drei**.

**الفكرة الجوهرية**: نمذجة إجرائية بالكود، لا استيراد GLB/Blender. كل شيء — الجدران، الأثاث، الأعمدة — مبني من `boxGeometry`/`cylinderGeometry`/`sphereGeometry`/`coneGeometry` مركّبة عبر مكوّن `Part` قابل لإعادة الاستخدام يستقبل `position/size/rotation/color/roughness/metalness/opacity`.

**البنية**: `Viewer3D → Canvas → FacilityScene` بأربع طبقات قابلة للتبديل: Shell (المبنى)، Spaces (الغرف كبيانات: `{id, name, position, size, color}` تُعاد على `RoomZones`)، Furniture، MEP. عرض منفجَّر (Exploded view) بإزاحة Y بسيطة لكل طبقة، بلا نظام فيزياء.

**الكاميرا**: `OrbitControls` من Drei + إعدادات مسبقة (ISO/TOP/FRONT/WALK) مع `camera.position.lerp(target, 0.14)` داخل `useFrame()` للانتقال الناعم بدل القفز.

**التفاعل**: كل عنصر ثلاثي الأبعاد يحمل `userData={{id, name}}` و`onClick`، فيصل الاختيار لحالة React ويُبرَز العنصر (emissive highlighting + outline + tooltip).

**التسميات والقياسات**: نصوص عربية وأزرار داخل المشهد عبر `<Html>` من Drei — أي HTML/CSS عادي معلَّق في الفضاء ثلاثي الأبعاد، لا نصوص Three.js. المسارات والقياسات عبر `<Line>` بمصفوفة إحداثيات + أقماع اتجاه.

**جودة العرض**: إضاءة متعددة (Ambient + Hemisphere + Directional + ثانوي)، ظلال بخريطة 1536×1536، أرضية بـ `<Grid>` + `<ContactShadows>`، ACES Filmic Tone Mapping، وزجاج بـ `meshPhysicalMaterial` (transmission/thickness) بدل شفافية بسيطة.

**مهم**: IFC/BIM في ذلك المشروع **لا يولّد الهندسة** — يُستخرَج منه فقط بيانات دلالية (عبر `parseStepDocument`/`extractIfcModel`) تُربَط بالمشهد الإجرائي عبر `element ID`. المشهد والبيانات خطّان منفصلان يلتقيان بمعرّف مشترك فقط.

---

## 10. الخطة لثلاثي الأبعاد هنا — القرار المعلَّق

طُلب صراحة: **لا تنفيذ الآن**، فقط الجاهزية. القرار الوحيد المطلوب قبل البدء:

**خيار أ — إجرائي (نفس أسلوب jawaz-al-imtithal)**
- يناسب لأن البيانات هنا شكلها مماثل فعلًا: أدوار/غرف كبيانات (`constructionFloors` في [constructionData.ts](client/src/constructionData.ts))، حالات ملوّنة، تفاعل نقر موجود مسبقًا في نسخة CSS الحالية.
- يُستبدَل تصميم الصندوق الحالي CSS بمكوّنات Three.js/R3F تدريجيًا، مع الحفاظ على نفس البيانات ونفس مكان الظهور (`/unit`).
- إعادة استخدام الأنماط الجاهزة من المشروع السابق: `Part` عام، نظام طبقات، كاميرا بإعدادات مسبقة + lerp، تسميات `<Html>`، عرض منفجَّر.

**خيار ب — نموذج حقيقي (GLB/IFC)**
- منطقي فقط إن وُجد ملف Blender/Revit/IFC فعلي لمبنى حقيقي. لا يوجد حاليًا في المستودع.

**التوصية الافتراضية**: الخيار أ، لأن لا يوجد نموذج حقيقي بحوزتنا والبيانات الحالية بنيت أصلًا كمخطط بيانات (parametric) يناسب النمذجة الإجرائية مباشرة.

**نقطة البدء الفعلية عند الإذن بالتنفيذ**:
1. تثبيت `three` + `@react-three/fiber` + `@react-three/drei`.
2. استبدال [UnitTwin.tsx](client/src/components/UnitTwin.tsx) بمكوّن `Viewer3D` جديد، بنفس واجهة الاستدعاء (`<ConstructionTwin />` / `<UnitTwin />`) حتى لا تتغيّر الصفحات المستدعية.
3. بناء `Part` عام أولًا، ثم تحويل `constructionFloors` الحالية لمشهد R3F بدل الصناديق CSS.
4. كاميرا OrbitControls + إعدادات مسبقة، تسميات عربية عبر `<Html>`.
5. اختبار أداء على الجوال قبل أي تفاصيل إضافية — أثاث/MEP لاحقًا إن طُلب.

---

## 11. ملفات مرجعية أخرى في المستودع

- [todo.md](todo.md) — سجل مهام تاريخي لتصميم الواجهة قبل هذه الجولة (كل البنود منجزة ✅، من مرحلة سابقة).
- [ideas.md](ideas.md) — وثيقة اتجاه بصري تاريخية (سكن 360، Tajawal) — **متجاوَزة جزئيًا**: الاسم الآن "يسر" والخط الآن Almarai، والباقي (الأخضر #0B4F46، فلسفة الحركة، مبدأ "لا قرار أهلية") ما زال ساريًا.
- [ux-recommendations.md](ux-recommendations.md) — لم يُراجَع هذه الجولة.
